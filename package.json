{
  "name": "project-name_stack",
  "private": true,
  "version": "0.0.0",
  "scheduleVersion": "0.0.0",
  "description": "DargStack configuration for <project_name>.",
  "repository": "https://github.com/<project_owner>/<project_name>_stack.git",
  "author": "<author_name> <<author_email>>",
  "license": "<license>",
  "dependencies": {},
  "devDependencies": {
    "@commitlint/cli": "17.0.3",
    "@commitlint/config-conventional": "17.0.3",
    "dargstack_rgen": "0.7.2",
    "husky": "8.0.1",
    "shx": "0.3.4"
  },
  "scripts": {
    "prepare": "if [[ -d '.git' && -d '.husky' ]]; then husky install && shx rm -rf .git/hooks && shx ln -s ../.husky .git/hooks; fi"
  }
}
